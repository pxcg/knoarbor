# Examples

This directory contains small inputs that new users can ingest to verify the full KnoArbor flow.

## Agent Loop Example

Use `agent-loop.md` to test:

1. Markdown source ingestion.
2. Wiki page generation.
3. Lint maintenance.
4. Query retrieval.

Suggested query:

```bash
uv run knoar query "Agent Loop 是什么？"
```
