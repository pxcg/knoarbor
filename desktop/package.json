{
  "name": "@knoarbor/desktop",
  "private": true,
  "version": "2.5.3",
  "description": "KnoArbor desktop app",
  "author": "pxcg",
  "license": "Apache-2.0",
  "type": "module",
  "main": "out/main/index.js",
  "scripts": {
    "build": "electron-vite build --ignoreConfigWarning && tsc -p tsconfig.json --noEmit",
    "build:renderer": "npm --prefix ../renderer run build",
    "build:service": "node scripts/build-service.mjs",
    "check:renderer": "node scripts/check-renderer.mjs",
    "clean:package": "node scripts/clean-package-artifacts.mjs",
    "prepare:package": "npm run clean:package && npm run build:renderer && npm run build:service && npm run build",
    "dist:mac": "npm run prepare:package && electron-builder --config electron-builder.config.cjs --mac dmg zip --publish never",
    "dist:win": "npm run prepare:package && npm run verify:icons && node scripts/run-windows-builder.mjs nsis",
    "test": "node --test tests/*.test.mjs",
    "dev": "npm run check:renderer && electron-vite dev",
    "pack:mac": "npm run prepare:package && CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --config electron-builder.config.cjs --mac dir",
    "pack:win": "npm run prepare:package && npm run verify:icons && node scripts/run-windows-builder.mjs dir",
    "preview": "electron-vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify:icons": "node scripts/verify-icon-assets.mjs"
  },
  "dependencies": {
    "@electron-toolkit/utils": "^4.0.0",
    "electron-log": "^5.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "7zip-bin": "5.2.0",
    "electron": "42.4.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^5.0.0",
    "typescript": "^5.6.3",
    "vite": "^7.3.6",
    "yaml": "^2.9.0"
  },
  "overrides": {
    "vite": {
      "esbuild": "0.28.1"
    }
  }
}
